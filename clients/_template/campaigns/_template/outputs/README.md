# Campaign Outputs

This folder records pointers to generated outputs when needed.

Generated run artifacts should normally remain outside the client workspace:

```text
output/run-{timestamp}/
```

Do not copy generated run folders here unless explicitly approved.
